Open demo_score.html in a modern browser.
Files expected: original.musicxml, generated.musicxml, original.mid, generated.mid.
Tip: If running in WSL, serve this folder via: python -m http.server -d viz 8000
Then visit http://localhost:8000/demo_score.html
